N皇后问题

搜索问题DFS

核心问题是
1. 构建棋盘
2. 标志数组
   因为是从上向下搜索,  row 这个维度不需要标记， 只需要标记cols 这个维度， 因为 y -> row 一旦使用， y + 1
   正对角线数组
   反对角线数组

根据 x, y 求对角线 位置    [x + y] 和 [x - y + n - 1]

DFS步骤
1. dfs : y = 0
2. if y == n :
        append
        return
   dfs x

DFS 要义 归去来兮
注意更新操作， 要修改对应的数组